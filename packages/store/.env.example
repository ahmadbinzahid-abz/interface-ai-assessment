# Operational database (runs, interventions, sessions, evidence index).
# Matches docker-compose.yml; `docker compose up -d` gives you exactly this.
DATABASE_URL="postgresql://cua:cua@localhost:5433/cua?schema=public"
