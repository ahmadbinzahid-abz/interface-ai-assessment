# Test database. Truncated between tests, so it must never point at the dev database.
# The guard in src/testing/setup.ts refuses to run unless the name ends in _test.
DATABASE_URL="postgresql://cua:cua@localhost:5433/cua_test?schema=public"
